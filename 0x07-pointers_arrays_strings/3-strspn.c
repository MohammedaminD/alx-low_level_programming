#include "main.h"
/**
 * _strspn -a function that gets the length of a prefix substring
 *
 * @s : char type pointer input variable
 * @accept : char type pointer input variable
 * Return:pointer value
 */
unsigned int _strspn(char *s, char *accept)
{
}
